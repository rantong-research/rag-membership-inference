# 复制本文件为 .env，并填入你自己的配置。
# 说明：.env 已被 .gitignore 忽略，不会提交到 Git。

# src/config.py 中 chat_model / api_key / base_url 为空时，会读取以下环境变量。
# 适用于任意 OpenAI 兼容接口（本地 vLLM / DashScope / OpenAI 等）。

chat_model=YOUR_MODEL_NAME
api_key=YOUR_API_KEY
base_url=YOUR_OPENAI_COMPATIBLE_BASE_URL
